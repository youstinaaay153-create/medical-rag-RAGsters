{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://instant-hackathon.local/schemas/grounded_response.json",
  "title": "GroundedResponse",
  "description": "Required output structure for every answer generated by the Day 3 grounded generation pipeline. Every field is mandatory. The model must be instructed (via the system prompt) to return exactly this shape.",
  "type": "object",
  "required": ["recommendation", "evidence", "citations", "confidence"],
  "additionalProperties": false,
  "properties": {
    "recommendation": {
      "type": "string",
      "minLength": 1,
      "description": "A short, direct answer to the clinical question in plain language. Must contain no claim that isn't supported by the evidence/citations below."
    },
    "evidence": {
      "type": "string",
      "description": "The exact retrieved excerpt(s) that support the recommendation, quoted or lightly trimmed — not paraphrased away from the source wording. Empty only when confidence is 'insufficient' (a refusal)."
    },
    "citations": {
      "type": "array",
      "description": "One entry per source excerpt used. Every non-refusal recommendation must be traceable to at least one citation. Empty only when confidence is 'insufficient' (a refusal).",
      "items": {
        "type": "object",
        "required": ["document", "page"],
        "additionalProperties": false,
        "properties": {
          "document": {
            "type": "string",
            "minLength": 1,
            "description": "Exact document name as it appears in your ingestion metadata (document_name field from Day 1)."
          },
          "section": {
            "type": "string",
            "description": "Section title or number if available in metadata, or 'N/A'."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "description": "Page number as an integer (page_number field from Day 1 metadata)."
          }
        }
      }
    },
    "confidence": {
      "type": "string",
      "enum": ["high", "medium", "low", "insufficient"],
      "description": "high: strong direct match in retrieved text. medium: partial/indirect match. low: weak or conflicting match. insufficient: below retrieval confidence threshold — recommendation must then be a refusal message, not a guess."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "confidence": { "const": "insufficient" } }
      },
      "else": {
        "properties": {
          "evidence": { "minLength": 1 },
          "citations": { "minItems": 1 }
        }
      }
    }
  ],
  "examples": [
    {
      "recommendation": "For adults with hypertension who need medication, start with a thiazide-type diuretic, an ACE inhibitor or ARB, or a long-acting calcium channel blocker.",
      "evidence": "WHO recommends the use of drugs from any of the following three classes of pharmacological antihypertensive medications as an initial treatment: (1) thiazide and thiazide-like agents; (2) angiotensin-converting enzyme inhibitors (ACEis)/angiotensin-receptor blockers (ARBs); (3) long-acting dihydropyridine calcium channel blockers (CCBs).",
      "citations": [
        { "document": "WHO_Hypertension_Guideline_2021", "section": "3.4 Drug classes to be used as first-line agents", "page": 8 }
      ],
      "confidence": "high"
    },
    {
      "recommendation": "I don't have enough information in the indexed guideline to answer this. This source covers hypertension treatment, not breast cancer screening.",
      "evidence": "",
      "citations": [],
      "confidence": "insufficient"
    }
  ]
}
